import { TaskConverterPipe } from './task-converter.pipe';

describe('TaskConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
